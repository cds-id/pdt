# Server
SERVER_PORT=8080

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=pdt
DB_PASSWORD=secret
DB_NAME=pdt

# JWT
JWT_SECRET=change-me-to-a-random-string
JWT_EXPIRY_HOURS=72

# Encryption (32-byte hex key for AES-256)
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Worker
SYNC_ENABLED=true
SYNC_INTERVAL_COMMITS=15m
SYNC_INTERVAL_JIRA=30m

# Reports
REPORT_AUTO_GENERATE=true
REPORT_AUTO_TIME=23:00

# R2 Storage (Cloudflare) — optional, reports saved to DB regardless
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_DOMAIN=
